# Raw Kaggle data and large derived/generated data files (regenerate via day1_eda.py /
# day2_model.py per README "How to Run" — not redistributed here)
data/*.csv

# Trained model artifact (regenerate via day2_model.py)
models/*.pkl

# Python
__pycache__/
*.pyc

# Local session config (absolute local paths, not portable)
.claude/

# Charts that embed a store's raw daily sales series (generated by the pipeline; the
# competition rules bar redistributing the data, so they stay local)
store1_timeseries.html
store1_acf_pacf.html
arima_baseline.html
